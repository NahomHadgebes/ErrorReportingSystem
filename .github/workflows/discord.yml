name: Notify Discord on Push

on:
  push:
    branches:
      - master   

jobs:
  notify:
    runs-on: ubuntu-latest

    steps:
      - name: Send message to Discord
        run: |
          curl -H "Content-Type: application/json" \
               -d "{\"content\": \"🚀 Ny kod pushad till *"ErrorReportingSystem"*!\"}" \
               ${{ secrets.ERRORREPORTINGSYSTEM_DEV }}
